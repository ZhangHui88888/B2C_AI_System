# Cloudflare Pages Redirects Configuration
# https://developers.cloudflare.com/pages/configuration/redirects/

# API proxy to Worker (uncomment and configure domain)
# /api/*  https://api.yourdomain.com/:splat  200

# Redirect www to non-www (configure in Cloudflare DNS instead for better performance)
# https://www.example.com/*  https://example.com/:splat  301

# Handle SPA routing (if needed - Astro generates static pages by default)
# /*  /index.html  200

# Legacy URL redirects
# /old-page  /new-page  301
# /products/old-slug  /products/new-slug  301
